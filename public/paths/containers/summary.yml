get:
  operationId: "getContainerSummary"
  tags:
    - Containers
  parameters:
    - name: containerId
      description: The ID of the requested container.
      in: path
      required: true
      schema:
        type: string
        #TODO can this be filtered and sorted  - is there a meta or includes?
  summary: Fetch Container Summary
  description: Requires the `containers-view` capability.
  responses:
    200:
      description: Returns an container summary resource.
      content:
        application/json:
          schema:
            type: object
            required: 
              - data
            properties:
              data:
                $ref: ../../../components/schemas/containers/summaries/ContainerSummary.yml
    default:
      $ref: ../../../components/responses/errors/DefaultError.yml
