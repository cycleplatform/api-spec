get:
  operationId: "getProviders"
  tags:
    - Providers
  parameters:
    - name: meta
      in: query
      required: false
      description:
        A comma separated list of meta values. Meta values will show up under a resource's
        `meta` field. In the case of applying a meta to a collection of resources, each
        resource will have it's own relevant meta data. In some rare cases, meta may not
        apply to individual resources, and may appear in the root document. These will be
        clearly labeled.
      schema:
        type: array
        items:
          type: string
          enum:
            - node
            - instances_count
    - $ref: "../../../parameters/SortParam.yml"
    - $ref: "../../../parameters/FilterParam.yml"
    - $ref: "../../../parameters/PageParam.yml"
  summary: List Providers
  description: Requires the `infrastructure-providers-view` capability.
  responses:
    "200":
      description: A collection of providers.
      content:
        application/json:
          schema:
            title: "ListProvidersReturn"
            type: object
            properties:
              data:
                type: array
                items:
                  "$ref": "../../../../schemas/infrastructure/providers/Provider.yml"
    "400":
      description: This usually means the data sent was in an invalid format.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
    "401":
      description: OAuth token is invalid.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
    "404":
      description: The requested resource doesn't exist.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
    "500":
      description: Indicates an issue with Cycle. Rare.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
post:
  operationId: "createProvider"
  tags:
    - Providers
  parameters: []
  summary: Create Provider
  description: Requires the `infrastructure-providers-manage` capability.
  requestBody:
    "$ref": "../../../../schemas/infrastructure/providers/requestBodies/CreateProviderRequest.yml"
  responses:
    "201":
      description: Returns a provider resource.
      content:
        application/json:
          schema:
            title: "ProviderCreateResponse"
            type: object
            properties:
              data:
                "$ref": "../../../../schemas/infrastructure/providers/Provider.yml"
    "400":
      description: This usually means the data sent was in an invalid format.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
    "401":
      description: OAuth token is invalid.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
    "404":
      description: The requested resource doesn't exist.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
    "500":
      description: Indicates an issue with Cycle. Rare.
      content:
        application/json:
          schema:
            "$ref": "../../../../schemas/error/ErrorResponse.yml"
