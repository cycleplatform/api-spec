description: Parameters for creating a new stack build.
content:
  application/json:
    schema:
      type: object
      properties:
        about:
          $ref: "../StackBuildAbout.yml"
        instructions:
          $ref: "../StackBuildInstructions.yml"
