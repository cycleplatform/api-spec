title: BackupState 
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the backup.
        type: string
        enum:
          - saving 
          - live 
          - deleting 
          - deleted 
  - "$ref": "../../State.yml"