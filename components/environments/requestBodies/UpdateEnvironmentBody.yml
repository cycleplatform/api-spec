---
description: Parameters for updating an environment.
content:
  application/json:
    schema:
      type: object
      properties:
        name:
          type: string
        about:
          "$ref": "../EnvironmentAbout.yml"