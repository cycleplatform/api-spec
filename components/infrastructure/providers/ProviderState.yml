title: ProviderState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the provider.
        type: string
        enum:
          - new
          - verifying
          - live
          - deleting
          - deleted
  - "$ref": "../../State.yml"
