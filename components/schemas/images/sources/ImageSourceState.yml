title: ImageSourceState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the image source.
        type: string
        enum:
          - live
          - deleting
          - deleted
  - $ref: "../../State.yml"
