title: ImageState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the image.
        type: string
        enum:
          - new
          - downloading
          - building
          - verifying
          - saving
          - live
          - deleting
          - deleted
  - "$ref": "../State.yml"
