title: PoolState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the Pool.
        type: string
        enum:
          - live
          - releasing
          - released
  - "$ref": "../../State.yml"
