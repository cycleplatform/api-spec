title: SanLunState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the LUN.
        type: string
        enum:
          - live
          - deleting
          - deleted
  - "$ref": "../../../State.yml"