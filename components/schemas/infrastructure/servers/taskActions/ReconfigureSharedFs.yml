title: ReconfigureSharedFs
type: object
required:
  - action
  - contents
properties:
  action:
    type: string
    description: The action to take.
    enum:
      - "reconfigure.sharedfs"
  contents:
    type: object
    properties:
      mounts:
        nullable: true
        type: object
        additionalProperties:
          type: object
          required:
            - type
            - options
            - source
          properties:
            type:
              type: string
              description: String describing the server mount type.
            options:
              type: string
              description: Comma separated string describing the server mount options.
              example: key1=value1,key2=value2
            source:
              type: string
              description: String describing the server mount source.
      directories:
        nullable: true
        type: object
        description: An object describing directory identifiers with key {}
        additionalProperties: {}
