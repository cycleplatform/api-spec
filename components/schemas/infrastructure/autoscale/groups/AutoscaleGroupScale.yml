title: AutoscaleGroupScale
type: object
required:
  - enable
properties:
  enable:
    type: boolean
  down:
    title: AutoscaleGroupScaleDown
    type: object
    properties:
      minimum:
        type: integer
      method:
        type: string
        enum:
          - fifo
          - lifo
  up:
    title: AutoscaleGroupScaleUp
    type: object
    properties:
      maximum:
        type: integer
