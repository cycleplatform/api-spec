title: AutoscaleGroupInfrastructure
type: object
required:
  - models
properties:
  models:
    type: array
    items:
      type: object
      required:
        - provider
        - id
        - priority
        - locations
      properties:
        provider:
          type: string
        id:
          type: string
        priority:
          type: integer
        location:
          type: array
          items:
            type: object
            required:
              - id
              - availability_zones
            properties:
              id:
                type: string
              availability_zones:
                type: array
                items:
                  type: string
