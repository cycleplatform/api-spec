title: LogsSearch
required:
  - type
  - match
properties:
  type:
    description: The input type used to query logs.
    type: string
    enum:
      - raw
      - regexp
  match:
    description: The search input used to query logs.
    type: string
