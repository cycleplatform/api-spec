title: ContainerReimageStep
type: object
description: Settings for reimaging a container in a pipeline 
required:
  - action
  - details
properties:
  action:
    type: string
    description: The action that the step takes.
    enum:
      - container.reimage
  details:
    allOf:
      - $ref: "../ResourceLocation.yml"
      - $ref: "../../containers/taskActions/Reimage.yml"
