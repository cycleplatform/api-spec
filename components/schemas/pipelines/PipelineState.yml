title: PipelineState
type: object
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the pipeline.
        type: string
        enum:
          - live
          - deleting
          - deleted
  - "$ref": "../State.yml"
