title: VirtualMachineConfig
type: object
description: |
  Defines the network and deployment configurations for a virtual machine.
properties:
  network:
    $ref: network/VirtualMachineNetworkConfig.yml
  deploy:
    $ref: deploy/VirtualMachineDeployConfig.yml
  resources:
    $ref: resources/VirtualMachineResourcesConfig.yml
  runtime:
    oneOf:
      - $ref: runtime/VirtualMachineRuntimeConfig.yml
      - type: "null"
required:
  - network
  - deploy
  - resources
