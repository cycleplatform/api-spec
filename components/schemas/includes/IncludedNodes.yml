title: IncludedNodes
description: |
  A map of nodes, keyed by ID, included on endpoints that support it when passing the matching includes query parameter.
type: object
additionalProperties:
  $ref: ../../../../../cycle-openapi/components/schemas/infrastructure/nodes/Node.yml
