title: IncludedLocations
description: |
  A map of locations, keyed by ID, included on endpoints that support it when passing the matching includes query parameter.
type: object
additionalProperties:
  $ref: ../infrastructure/providers/ProviderLocation.yml
