title: IncludedStacks
description: |
  A map of stacks, keyed by ID, included on endpoints that support it when passing the matching includes query parameter.
type: object
additionalProperties:
  $ref: ../stacks/Stack.yml
