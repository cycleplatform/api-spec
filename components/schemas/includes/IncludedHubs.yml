title: IncludedHubs
type: object
description: |
  A map of hubs, keyed by ID, included on endpoints that support it when passing the matching includes query parameter.
additionalProperties:
  $ref: ../hubs/Hub.yml
