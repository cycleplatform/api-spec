title: DnsRecordLinkedRouting
type: object
description: Rules for routing traffic at the load balancer for this linked record.
properties:
  http:
    oneOf:
      - $ref: DnsRecordLinkedHttpRouting.yml
      - type: "null"
  traffic_splitting:
    $ref: ./DnsRecordLinkedTrafficSplitting.yml
