title: ScaleThresholdMetricNetworkRequests
type: object
required:
  - type
  - details
properties:
  type:
    type: string
    enum:
      - network-requests
  details:
    type: object
    required:
      - requests_total
    properties:
      requests_total:
        type: integer
