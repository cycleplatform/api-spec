title: IntegrationDefinitionField
type: object
required:
  - name
  - required
properties:
  regex:
    type:
      - string
      - "null"
  required:
    type: boolean
  name:
    type: string
  description:
    type: string
  type:
    type: string
    enum:
      - "select"
      - "string"
      - "int"
      - "blob"
  options:
    oneOf:
      - type: array
        items:
          type: string
      - type: "null"
