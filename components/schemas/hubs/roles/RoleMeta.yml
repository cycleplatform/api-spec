title: RoleMeta
type: object
description: A list of meta fields that can be applied to a role.
properties:
  counts:
    type: object
    required:
      - members
      - api_keys
    properties:
      members:
        type: integer
      api_keys:
        type: integer
    
