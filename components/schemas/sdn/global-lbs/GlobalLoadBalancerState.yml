title: GlobalLoadBalancerState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the global load balancer.
        type: string
        enum:
          - new
          - live
          - deleting
          - deleted
  - $ref: ../../State.yml
