title: TaskState
type: object
properties:
  changed:
    $ref: ../DateTime.yml
  current:
    description: The current state of the task.
    type: string
    enum:
      - pending
      - error
      - running
      - completed
