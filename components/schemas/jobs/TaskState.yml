title: TaskState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the task.
        type: string
        enum:
          - pending
          - error
          - running
          - completed
  - "$ref": "../State.yml"
