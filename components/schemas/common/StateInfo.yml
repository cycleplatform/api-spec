title: StateInfo
type: object
description: Information pertaining to a resources state
required:
  - changed
  - current
properties:
  current:
    description: The current state
    type: string
    enum:
      - new
      - offline
      - decomissioned
      - online
  changed:
    "$ref": "../DateTime.yml"
  error:
    "$ref": "../errors/Error.yml"
