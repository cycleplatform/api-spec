title: StackState
allOf:
  - required:
      - current
    properties:
      current:
        description: The current state of the stack.
        type: string
        enum:
          - live
          - deleting
          - deleted
  - $ref: "../State.yml"
