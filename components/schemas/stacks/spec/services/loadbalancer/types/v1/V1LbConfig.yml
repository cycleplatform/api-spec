title: V1LbConfig
type: object
required:
  - controllers
properties:
  controllers:
    type: array
    description: A configuration for a specific port.
    items:
      $ref: ./V1LbController.yml
  controller_template:
    allOf:
      - type: object
        nullable: true
      - $ref: ./V1LbController.yml
