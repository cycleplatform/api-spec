description: Parameters for deleting a server
content:
  application/json:
    schema:
      type: object
      required:
        - options
      properties:
        options:
          type: object
          description: described the options for removing server
          required:
            - force
          properties:
            force:
              description: Boolean that dictates force delete
              type: boolean
