title: JobState
allOf:
  - required:
      - current
    properties:
      current:
        description: the current state of the job.
        type: string
        enum:
          - new
          - queued 
          - error 
          - scheduled 
          - expired 
          - running 
          - completed  
  - "$ref": "../State.yml"