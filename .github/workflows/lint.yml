name: Run lint on merge main
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install
        run: yarn add
      - name: Run OpenAPI Linter
        run: yarn lint
